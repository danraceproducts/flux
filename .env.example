# Firebase Configuration
# =====================
# Option 1: Service Account JSON as string (recommended for Vercel)
# Copy your Firebase service account JSON and paste it as a single line
FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project-id",...}'

# Option 2: Path to service account JSON file (for local development)
# FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/serviceAccountKey.json

# Option 3: Default Application Credentials (for GCP deployment)
# No configuration needed - automatically uses GCP credentials

# Server Configuration
# ====================
PORT=3000
NODE_ENV=development
